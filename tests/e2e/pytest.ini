[pytest]
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    e2e:  End-to-end tests requiring full running stack
    slow: Tests taking >10 seconds

addopts =
    -v
    --tb=short
    -p no:warnings

# Allure environment properties written to results dir
# so reports show stack info
